# RegisterTMR replicates the FF only, then votes its output. The XOR remains shared.
read_verilog top.v
hierarchy -check -top top
proc; opt
tmrx_mark
tmrx -c tmrx_config.toml

select -assert-count 3 top/t:$adff
select -assert-count 1 top/t:$xor
