= Quick Start
:description: Get up and running with TMRX in three steps.

This guide walks you through the shortest path to a working TMRX run.
For the reasons behind the pass order, see xref:yosys-integration.adoc[Yosys Integration].

== Procedure

. Create a configuration file `tmrx_config.toml`:
+
[source,toml]
----
[global]
tmr_mode = "LogicTMR"
insert_voter_after_ff = true
clock_port_names = ["clk_i", "clk"]
reset_port_names = ["rst_ni", "rst_n"]
----

. Create a Yosys synthesis script `synth.ys`:
+
[source]
----
read_verilog my_design.v
hierarchy -check -top top

proc; opt
techmap

tmrx_mark

read_liberty -lib cells.lib
dfflibmap -liberty cells.lib
abc -liberty cells.lib

tmrx -c tmrx_config.toml

opt -noff
techmap
dfflibmap -liberty cells.lib
abc -liberty cells.lib

write_verilog -noattr output.v
----

. Run Yosys with the TMRX plugin:
+
[source,bash]
----
yosys -m build/tmrx.so -s synth.ys
----

== Next Steps

* Learn about xref:tmr-strategies.adoc[TMR Strategies] and when to use each one.
* Explore the xref:configuration.adoc[Configuration] guide and the xref:configuration-reference.adoc[Configuration Reference].
* See complete xref:examples.adoc[Examples] including hierarchical designs.
